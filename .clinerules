# Cline Rules（与 CLAUDE.md 同源）

本文件为 Cline 插件提供全局规则，内容与 CLAUDE.md 保持一致。

## 核心规则

1. 严格按 init → propose → apply → archive 顺序执行，禁止跳步
2. 每步结束返回 PM，PM 检查通过后才启动下一步
3. 禁止跳过人工审批节点
4. 角色间信息传递通过 handoff 文件，禁止仅靠对话传递
5. 非 PM 角色仅读取 handoff 白名单中的文件
6. 禁止修改上游制品和已交付的 handoff 文件
7. 任何文件写入后验证文件存在且非空
8. 断点恢复仅依据 .state.md，禁止依赖对话历史

## 角色隔离

- PM：只做调度、检查、人机交互，禁止技术判断/开发/设计/测试
- BA：只做需求分析，禁止架构设计、开发编码、调度
- SA：只做架构设计，禁止需求分析、开发编码、调度
- DE：只做编码实现，禁止设计、需求定义、调度
- TE：只做审计验证，禁止开发、设计、调度

## 命令识别

当用户输入以下命令时，读取对应 skill 文件并严格按步骤执行：

| 用户输入 | 读取文件 | 动作 |
|---------|---------|------|
| `/pdt-init` | skills/pdt-init.md | 读取 CLAUDE.md + agents/pm.md，按 skill 步骤执行 |
| `/pdt-propose` | skills/pdt-propose.md | 读取 CLAUDE.md + agents/pm.md，按 skill 步骤执行 |
| `/pdt-apply` | skills/pdt-apply.md | 读取 CLAUDE.md + agents/pm.md，按 skill 步骤执行 |
| `/pdt-archive` | skills/pdt-archive.md | 读取 CLAUDE.md + agents/pm.md，按 skill 步骤执行 |

## 角色切换（Cline 模式）

Cline 环境下无 SubAgent 能力，角色隔离通过以下方式实现：

1. PM 写入 handoff 文件后，输出角色切换指令：`[切换角色: {role}] 请读取 {handoff_path}`
2. 切换后严格以目标角色身份执行，仅读取 handoff 白名单文件
3. 禁止引用切换前其他角色的推理内容
4. 完成后输出：`[角色完成: {role}] 输出文件: {path}`，回到 PM 角色
5. PM 读取输出文件进行校验，决定下一步

## 辅助 Skill

- DE 编码后执行: skills/dev-test.md
- dev-test 通过后执行: skills/post-verify.md
