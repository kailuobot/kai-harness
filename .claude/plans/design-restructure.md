# 计划：重构 docs/design.md

## 目标
参考 ref-design.md 的目录结构重写 design.md，吸收其优秀设计理念，同时保留今天四轮优化的所有内容。

## ref-design 的目录结构（参考）
1. 整体设计目标（核心诉求 + 设计原则）
2. 一级架构总览（模块全景 + 目录结构）
3. SubAgent 设计（角色定义 + 契约结构）
4. 开发流程设计（PM调度 + 流程定义 + Profile + 状态机 + SR Gate + 崩溃恢复 + 并行）
5. 上下文管控设计（隔离机制 + Prompt构成 + Handoff协议）
6. Skill 设计（文件位置 + 格式 + 与Agent关系）
7. 质量管控设计（验收体系 + 检查器 + 自修复 + 质量门禁）
8. 度量与记忆设计
9. 工具体系设计
10. 模板体系
11. 团队分工与并行开发

## mini-harness 的新 design.md 目录结构（适配版）
1. 整体设计目标（核心诉求 + 四层防线设计原则）
2. 一级架构总览（模块全景 + 目录结构）
3. 角色与隔离设计（六角色 + 双模式隔离 + PM主会话模型）
4. 开发流程设计（PM调度协议 + 四阶段流程 + 三档Mode + 状态机 + SR Gate标准 + 断点恢复 + 并行批次）
5. 上下文管控设计（隔离机制 + SubAgent Prompt构成 + Handoff协议）
6. Skill 设计（文件位置 + 与Agent关系 + Auto/Manual模式）
7. 质量管控设计（三层质量注入 + PM质量门禁 + 修复收敛 + 硬校验脚本 + 金标准示例）
8. 审批决策设计（SR Gate + 决策上下文 + 审批呈现格式）
9. 模板体系（handoff + state + 日志 + 产出结构参考 + 金标准示例）
10. 产出类型体系（output_type + test_strategy + 环境检测）
11. PPT 子系统

## 从 ref-design 吸收的优秀理念
- SR Gate 明确的通过标准（criteria）
- "PM调度 + 硬护栏" 的职责分离表述
- 六条流程铁律的精炼表达
- 三层验收体系（L1格式 + L2内容 + L3工程）的概念映射到我们的 A/B/C/D 类检查
- 兜底策略（组件未就绪时的降级方案）
- 停止条件概念（PM何时暂停）

## 实施方式
完全重写 docs/design.md，从头构建。保留权威声明。
预计 ~300 行（精炼但完整）。
