读取 skills/mh-ppt.md 的完整内容，严格按照其中定义的步骤执行。

本命令是 output_type=ppt 的快捷入口。执行时自动设置 output_type=ppt，后续流程遵循主流程 + PPT 补充规则。

执行前先读取 CLAUDE.md 确认全局纪律，读取 agents/pm.md 确认 PM 角色契约，读取 agents/designer.md 确认 Designer 角色契约。
